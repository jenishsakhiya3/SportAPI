apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: temp
  labels:
    app.kubernetes.io/name: temp
    app.kubernetes.io/part-of: temp-project
    kubernetes.azure.com/generator: devhub
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: temp
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80