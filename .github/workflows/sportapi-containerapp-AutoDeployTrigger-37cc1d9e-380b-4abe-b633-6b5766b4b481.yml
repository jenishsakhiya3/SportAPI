name: Trigger auto deployment for sportapi-containerapp

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ sport-api ]
    paths:
    - '**'
    - '.github/workflows/sportapi-containerapp-AutoDeployTrigger-37cc1d9e-380b-4abe-b633-6b5766b4b481.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.SPORTAPICONTAINERAPP_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SPORTAPICONTAINERAPP_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SPORTAPICONTAINERAPP_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: 
          registryUsername: ${{ secrets.SPORTAPICONTAINERAPP_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SPORTAPICONTAINERAPP_REGISTRY_PASSWORD }}
          containerAppName: sportapi-containerapp
          resourceGroup: SportEastAsia
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


