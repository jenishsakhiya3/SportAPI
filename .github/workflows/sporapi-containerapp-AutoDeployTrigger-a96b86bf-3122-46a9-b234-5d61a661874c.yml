name: Trigger auto deployment for sporapi-containerapp

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ sport-api ]
    paths:
    - '**'
    - '.github/workflows/sporapi-containerapp-AutoDeployTrigger-a96b86bf-3122-46a9-b234-5d61a661874c.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.SPORAPICONTAINERAPP_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SPORAPICONTAINERAPP_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SPORAPICONTAINERAPP_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: 
          registryUsername: ${{ secrets.SPORAPICONTAINERAPP_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SPORAPICONTAINERAPP_REGISTRY_PASSWORD }}
          containerAppName: sporapi-containerapp
          resourceGroup: SportEastAsia
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


